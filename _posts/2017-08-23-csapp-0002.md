---
layout: post
title: "CS:APP Information and its Correlate"
date: 2017-08-23 10:00
category: "design"
tag: CSAPP
---

> Claude Shannon :
<br>
Information:
* the negative reciprocal value of probability
* the resolution of uncertainty
<br>
------ 《Informaion Theorem》

<br>
*CSAPP Chapter 2: Representing and Manipulating Information*

## 信息存储
- 大多数计算机使用8位的块或者**字节**作为最小的可寻址的内存单位，而不是访问内存中单独的位
- 机器级程序将内存视为一个非常大的字节数组，称为**虚拟内存**
- 内存中的每个字节都由一个唯一的数字来标识，称为它的**地址**
- 所有可能地址的集合就称为**虚拟地址空间**

<br>
> 写给C语言初学者 (假装有C语言初学者的读者）
> * 指针是C语言的一个重要特性，它提供了引用数据结构（包括数组）的元素的机制。它与普通变量一样有两个重要的属性：值和类型。它的值表示某个对象的位置，而它的类型表示那个位置上所储存对象的类型。

<br>


### 常用的进制转换
- 如十六进制（hexadecimal，hex），十进制（decimal，dec），八进制（octal，oct），二进制（binary，bit）之间的，使用对应类型的科学计数法转换即可，当然不同进制之间的转换可能会出现精度损失（e.g. float，double）
- 当然，较大数值（>  1<<64）之间的转换还是使用现有的程序比较好（当初C++实验时写十六进制到十进制的大数转换写（debug）了三个晚自习唉#

### 字数据大小
- 每台计算机都有一个**字长**，指明指针数据的**标称大小**，而虚拟地址就是以这样的一个字来编码的，所以虚拟地址空间的最大大小是由字长来决定的。
- 下表给出了C语言数据类型在不同字长下的典型大小

--------------------------------------


 &emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;|C声明|   &emsp;&emsp;&emsp;&emsp;&emsp;&emsp;32位 | &emsp;64位 |
		:----:|:----:|:----:|:----:
| char                       |   &emsp;&emsp;&emsp;&emsp;&emsp;&emsp;1    |  &emsp;1 |
| short                      |   &emsp;&emsp;&emsp;&emsp;&emsp;&emsp;2    |  &emsp;2 |
| int                        |   &emsp;&emsp;&emsp;&emsp;&emsp; &emsp;4   |  &emsp;4 |
| long                       |   &emsp;&emsp;&emsp;&emsp;&emsp;&emsp;4    |  &emsp;8 |
| int_32t                    |   &emsp;&emsp;&emsp;&emsp;&emsp;&emsp;4    |  &emsp;4 |
| int_34t                    |   &emsp;&emsp;&emsp;&emsp;&emsp;&emsp;8    |  &emsp;8 |
| char*                      |   &emsp;&emsp;&emsp;&emsp;&emsp;&emsp;4    |  &emsp;8 |
| float                      |   &emsp;&emsp;&emsp;&emsp;&emsp;&emsp;4    |  &emsp;4 |
| double                     |  &emsp;&emsp;&emsp;&emsp;&emsp; &emsp;8    |  &emsp;8 |
 
 --------------------------------------
 
<br>
### 寻址和字节顺序
- 对于跨越许多字节的程序对象，我们必须建立两个规则：这个对象的地址是什么，以及在内存中如何排列这些字节
- 排列表示一个对象的字节有两个通用的规则
    - 考虑一个$$w$$为的整数，其各位表示为$$[ x_{w-1}, x_{w-2}, ..., x_1, x_0 ]$$ ,假设$$w$$是8的倍数，这些位就能被分组成为字节，其中最高
	  有效字节包含位$$[ x_{w-1}, x_{w-2}, ..., x_{w-7}, x_{w-8} ]$$,最低有效位包含$$[ x_7, x_6, ..., x_1, x_0 ]$$。
    - 某些机器在选择内存中按照从最低有效字节到最高有效字节的顺序存储对象（**小端法**），而另一些则相反（**大端法**）
	  。非常奇怪的是，人们又对采用哪个方法比较合适这个问题上展开了争论，就和大括号换行一样（无聊）
		- 但是目前来看intel，ARM上的IOS和Android都是小端（tao	
		
> <small><small>作者: 只要选择了一种规则并且始终如一地坚持，对于哪种字节排序的选择都是任意的  

- 字节顺序一般来说并不重要，除了
	- 大端小端机器之间传输数据时
	- 阅读表示整数数据的字节顺序时
	- 编写规避正常的类型系统的程序时
- 以下代码说明了第三种情况

``` c
#include <stdio.h>

typedef unsigned char* byte_pointer;

void show_bytes(byte_pointer start, size_t len)
{
	size_t i;
	for(i=0; i<len; ++i)
		printf(" %.2x", start[i]);
	printf("\n");
}

void show_int(int x)
{
	show_bytes((byte_pointer) &x, sizeof(int));
}

void show_float(float x)
{
	show_bytes((byte_pointer) &x, sizeof(float));
}

void show_pointer(void* x)
{
	show_bytes((byte_pointer) &x, sizeof(void*));
}
```
