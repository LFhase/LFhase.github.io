---
layout: post
comments: true
title: "CS:APP Wandering OS "
date: 2017-08-11 15:23
categories: csapp


---


> Gene Amdahl :<br>
A program (or algorithm) which can be parallelized can be split up into two parts:
* A part which cannot be parallelized;
* A part which can be parallelized;
<br>
------ Amdahl' Law

<br>
*CS:APP  chapter one Wandering OS*
<br>

## 信息就是位+上下文
 - 系统中所有信息都由比特存储
 - 区分不同数据对象的唯一方法是它们的上下文

## 系统的硬件组成
- 总线
- I/O设备：键盘、鼠标、显示器、硬盘
- 主存： 临时存储设备（DRAM）
- 处理器

## 操作系统管理硬件
- 防止硬件被失控的应用程序滥用
- 向应用程序提供简单一致的机制来控制复杂而又通常大小不同的低级硬件设备
- **操作系统通过几个基本的抽象概念（进程、虚拟内存和文件）来实现这两个功能**
 - 进程
     - 操作系统对一个正在运行的程序的一种抽象
     - 并发运行是指一个进程的指令和另一个进程的指令是交错执行的
     - 操作系统保持跟踪进程运行所需要的所有状态信息，即所谓**上下文**
     - 从一个进程到另一个进程的转换是由操作系统内核管理的
        - 内核是系统管理全部进程所用代码和数据结构的集合
 - 线程
 - 虚拟内存
       - 虚拟内存是一个抽象概念，它为每个进程提供了一个假象，即每个进程都在                  <br>独占地使用主存，每个进程看到的内存都是一致的，称为虚拟地址空间。
 - 文件
      - 文件就是字节序列，每个I/O设备，包括磁盘、键盘、显示器，甚至网络，都
         可以看成是文件
      - 它向应用程序提供了一个统一的视图，来看待系统中可能含有的所有各式各样的
         I/O设备

## 系统之间利用网络通信
## Amdahl定律
- 首先我们定义系统执行某程序需要时间**T<sub>old</sub>**
- 假设系统某部分所需要执行时间占总时间的比例为$$a$$，而该部分性能提升比例为$$k$$
- 令加速后的执行时间为**T<sub>new</sub>**
- 我们可以计算加速比$$S = \frac 1{(1-a)+\frac a{k}}$$
<br>
*补充：性能提示最好的表示方法就是用比例的形式 **T<sub>old</sub>/T<sub>new</sub>** *，而我们用后缀‘X’来表示比例，如‘2.2X’读作‘ 2.2倍’*
- Amdahl定律描述了改善任何过程的一般原则
<br>
*(增加了MathJax来显示公式-2017-08-21)*

## 并发和并行

*数字计算机的整个历史中，有两个需求是驱动进步的持续动力，一个是我们想要计算机做的更多，另一个是我们想要计算机运行的更快*
- 术语**并发**是一个通用的概念，指一个同时具有多个活动的系统
- **并行**指的是用并发来使一个系统运行得更快
- 我们按照系统层次结构中由高到低的顺序重点强调三个层次
  - **线程级并发**
    - 使用线程，我们甚至能够在一个进程中执行多个控制流
    - 传统意义上，并发执行只是模拟出来的，即计算机在它正在执行的进程间快速切换实现
    - 以前，即使处理器必须在多个任务间切换，大多数实际的计算也都是由一个处理器完成的，这种配置称为**单处理器系统**
    - 当构建一个由单操作系统内核控制的多处理器组成的系统时，我们就得到了一个**多处理器系统**
    - **超线程**，有时称为**同时多线程**，是一项允许一个CPU执行多个控制流的技术<br>（比如Intel Core i7
    - 多处理器的使用可以从两个方面提升系统性能<br>1. 它减少了在执行多个任务时模拟并发的需要<br>2. 它可以使应用程序运行得更快
  - 指令级并行
    - 在较低的抽象层次上，线代处理器可以同时执行多条指令的属性称为指令级并行
  - 单指令、多数据并行
    - 在最低层次上，许多现代处理器拥有特殊的硬件，允许一条指令产生多个可以并行执行的操作，这种方式称为单指令、多数据，即**SMD并行**<br>（比如较新的几代Intel和AMD都具有并行地对8对单精度浮点数做加法的指令

## 计算机系统中抽象的重要性
- **抽象**的使用是计算机科学中最为重要的概念之一 <br>比如各种API，程序员无须理解它内部的工作便可以使(ctrl c)用(ctrl v)这些代码
- 在处理器中，**指令集架构**提供了对实际处理器硬件的抽象，使用这个抽象，机器代码程序表现得就好像运行在一个一次只执行一条指令的处理器上
- **文件**是对I/O设备的抽象
- **虚拟内存**是对程序存储器的抽象
- **进程**是对一个正在运行的程序的抽象
- **虚拟机**提供对整个计算机的抽象，包括操作系统、处理器和程序

<br>正如《计算机科学概论》（第三版）中所提到的：
> 抽象是计算的基础

<br>由此结束CSAPP中第一章的学习

*ps: 很多图以目前我的markdown+latex语法真的画不出来，接下来试着通过一些方法来实现博客中图片的添加<br>（希望不要超过githubpage的容量#*
